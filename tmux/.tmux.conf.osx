if -b 'command -v reattach-to-user-namespace > /dev/null 2>&1' 'bind-key -T copy-mode C-c send-keys -X copy-pipe "tmux save-buffer - | reattach-to-user-namespace copy-text" \; display-message "copied to system clipboard" \; send-keys -X cancel'