---
name: scout
description: Use for read-only exploration and search — locating code, enumerating call sites, mapping where a concern lives across a repo. Holds no editing or command-execution tools, so it is the safe tier for wide sweeps whose findings the caller acts on itself. Not for making changes.
tools: Read, Grep, Glob
model: haiku
skills:
  - scout
---
